import React from 'react';
import Darkmode from '../../../Components/Darkmode/Darkmode';

function Settings() {
    return (
        <div>
            <h1>Settings </h1>
            <Darkmode/>
        </div>
    );
}

export default Settings;
